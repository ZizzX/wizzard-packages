import { App } from '@examples/quickstart/src/App';

import { restartable } from '../components/StageBoundary';

/** The quickstart on the React binding, mounted beside its source. */
export default restartable(App);
