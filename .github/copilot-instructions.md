# Contributor instructions

All rules for this repository live in [`AGENTS.md`](../AGENTS.md). Read that file.

Nothing is duplicated here on purpose: the previous split between `AGENTS.md`,
`.agent/rules/` and `.github/instructions/` produced rules that contradicted each other.
