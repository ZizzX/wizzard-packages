{
  "name": "@examples/next-app",
  "private": true,
  "version": "0.0.0",
  "description": "A Next.js App Router fixture. `/` mounts the wizard from a server component, to prove the binding's 'use client' directive survives the package build; `/devtools` mounts the devtools panel over a flow that can refuse a move.",
  "scripts": {
    "build": "next build",
    "start": "next start",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@wizzard-packages/core": "workspace:*",
    "@wizzard-packages/devtools": "workspace:*",
    "@wizzard-packages/react": "workspace:*",
    "next": "^16.3.4",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3"
  }
}
