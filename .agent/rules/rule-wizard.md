---
trigger: always_on
---

All contributor rules live in `AGENTS.md` at the repository root. Read that file.

This file exists only so editors that auto-load `.agent/rules/*` pick the rules up.
Do not add rules here — a rule in two places is a rule that will disagree with itself.
